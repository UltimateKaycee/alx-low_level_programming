#include "main.h"

/**
 * main - function to check code
 *
 * Return: Always 0.
 */
int main(void)
{
	char *words;

	words = "ALX is a blessing to those who have lond had interest in learning software engineering";

	print_rev(str);
	return (0);
}
