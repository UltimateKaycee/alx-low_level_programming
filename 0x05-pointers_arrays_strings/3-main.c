#include "main.h"

/**
 * main - function checks code
 *
 * Return: Always 0.
 */
int main(void)
{
    char *words;

    words = "ALX is a blessing for those who have long had ineterst in learning software engineering"; 

    _puts(words);

    return (0);
}
